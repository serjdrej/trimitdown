name: Build macOS app

on:
  push:
    branches: [main]
  workflow_dispatch:
  # Позволяет release.yml вызвать эту сборку вместо копии её шагов: копия
  # разошлась бы с оригиналом молча, а расхождение спек этот проект уже проходил.
  workflow_call:

jobs:
  build:
    strategy:
      matrix:
        include:
          - runner: macos-15-intel
            arch: x86_64
          # macos-14 entered deprecation on 2026-07-06 and stops working on
          # 2026-11-02; workflows still using the label are terminated with an
          # error then (actions/runner-images#13518). Moved before it expires
          # rather than after, and onto the same OS version as the Intel job so
          # the two bundles differ in architecture and nothing else.
          - runner: macos-15
            arch: arm64
    runs-on: ${{ matrix.runner }}
    # Явно, а не по умолчанию репозитория: сборке нужно только читать код.
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      # The iconset is derived from mac-build/icon_master_1024.png and is not
      # committed; Pillow arrives transitively with pdfplumber.
      - name: Generate iconset
        run: python generate_iconset.py

      - name: Build .icns from iconset
        run: iconutil -c icns mac-build/AppIcon.iconset -o mac-build/AppIcon.icns

      - name: Build app with PyInstaller
        env:
          PYI_TARGET_ARCH: ${{ matrix.arch }}
        run: pyinstaller main.spec

      # A green build proves nothing about what is inside the bundle: a missing
      # module produces an app that starts fine and fails on the first PDF. Same
      # check the tiktoken_ext regression needed. Path is explicit -- PyInstaller
      # names the build directory after the spec, and a stale build/<other>/ dir
      # would make a glob answer for the wrong build.
      - name: Verify package contents are in the bundle
        run: |
          grep -q "trimitdown_pdf" build/main/Analysis-00.toc \
            || { echo "trimitdown_pdf missing from the bundle"; exit 1; }
          grep -Eq "'trimitdown(\.[A-Za-z_][A-Za-z0-9_]*)?'" build/main/Analysis-00.toc \
            || { echo "trimitdown package missing from the bundle"; exit 1; }
          grep -q "tiktoken_cache" build/main/Analysis-00.toc \
            || { echo "tiktoken cache missing from the bundle"; exit 1; }

      # ВРЕМЕННО, снять после починки: измерение до правки. Гипотеза -- в бандл
      # приезжают две разные OpenSSL под одним базовым именем libssl.3.dylib
      # (одна от Python, другая от cryptography), PyInstaller оставляет одну и
      # переписывает ссылку _rust.abi3.so на неё. Чинить наугад дешевле только
      # на вид: неверный диагноз даёт правку, которая закрывает симптом здесь и
      # оставляет его на другой версии раннера.
      - name: Diagnose the OpenSSL pair (temporary)
        run: |
          echo "=== interpreter and its OpenSSL ==="
          python -c "import ssl, sys; print(sys.version); print(ssl.OPENSSL_VERSION)"
          echo "=== cryptography as installed ==="
          python -c "import cryptography; print(cryptography.__version__)"
          rust_so=$(python -c "import cryptography.hazmat.bindings._rust as m; print(m.__file__)")
          otool -L "$rust_so"
          echo "=== every libssl/libcrypto inside the bundle ==="
          find dist/TrimItDown.app -name 'libssl*' -o -name 'libcrypto*' | while read -r lib; do
            echo "-- $lib"
            otool -L "$lib" | sed -n '2,3p'
            echo "   defines _SSL_get0_group_name: $(nm -gU "$lib" 2>/dev/null | grep -c 'SSL_get0_group_name')"
          done
          echo "=== _rust.abi3.so inside the bundle ==="
          find dist/TrimItDown.app -name '_rust.abi3.so' -exec otool -L {} \;
          echo "=== resolved dependency versions ==="
          python -m pip freeze

      # Дымовой документ строится тем же генератором, что и фикстуры сюиты:
      # проза плюс настоящая таблица, то есть движок обязан отработать. Модуль
      # чистый, без импортов -- сборочной джобе не нужны dev-зависимости.
      - name: Build the smoke document
        run: python -c "from tests.pdf_fixtures import ruled_table; open('smoke.pdf','wb').write(ruled_table())"

      # ЭТО главная проверка сборки, а не соседняя проверка содержимого. Греп по
      # Analysis-00.toc смотрит на имена файлов и не видит, что процесс умирает
      # на первом dlopen -- ровно так все macOS-бандлы до 0.1.1 уезжали к
      # пользователям неработающими. Здесь запускается сам артефакт и делает
      # настоящую конверсию.
      #
      # Вердикт берётся из лога, а не из кода возврата: у windowed-процесса он
      # доходит до шелла не на всякой платформе, и проверка обязана быть одной и
      # той же в обеих джобах. Код возврата печатается рядом как сведение.
      - name: Smoke-launch the bundle
        env:
          # Относительные пути намеренно: на Windows-раннере $PWD в bash это
          # /d/a/..., чего интерпретатор внутри бандла открыть не может, а
          # проверка обязана быть буквально одинаковой в обеих джобах.
          TRIMITDOWN_SMOKE_LOG: smoke.log
        run: |
          set +e
          TRIMITDOWN_SMOKE=smoke.pdf dist/TrimItDown.app/Contents/MacOS/TrimItDown
          status=$?
          set -e
          echo "exit status: $status"
          test -s smoke.log || { echo "the bundle produced no smoke log at all"; exit 1; }
          cat smoke.log
          tail -1 smoke.log | grep -q '^smoke ok:' \
            || { echo "the bundle did not complete a conversion"; exit 1; }

      - name: Zip .app bundle
        run: ditto -c -k --sequesterRsrc --keepParent dist/TrimItDown.app TrimItDown-macOS-${{ matrix.arch }}.zip

      - uses: actions/upload-artifact@v7
        with:
          name: TrimItDown-macOS-${{ matrix.arch }}
          path: TrimItDown-macOS-${{ matrix.arch }}.zip
