*Читать на: русском (этот файл) · [English](README.en.md)*

# TrimItDown Docker-сервис

FastAPI + [MarkItDown](https://github.com/microsoft/markitdown) на Docker: конвертация файлов
в Markdown, архив с поиском и удалением, HTTPS с самоподписанным сертификатом. Тот же сервис
раздаёт PWA — на iPhone открывается как обычный сайт и ставится на экран «Домой» полноценным
приложением (офлайн-кеш интерфейса через service worker, иконка).

Разворачивается на любой машине с Docker — домашний NAS, VPS, свой сервер. Папка называется
`docker-server/`, потому что подходит для любого сервера с Docker, а не только для NAS.

## Деплой

```bash
# на машине, где будет крутиться сервис (NAS, VPS, свой сервер — что угодно с Docker)
mkdir -p certs archive
cd certs
openssl req -x509 -nodes -newkey rsa:2048 \
  -keyout key.pem -out cert.pem -days 3650 \
  -subj "/CN=trimitdown" \
  -addext "subjectAltName=IP:ТВОЙ_IP_СЕРВЕРА"
cd ..
sudo docker-compose up -d --build
```

`docker-compose.yaml` пробрасывает `8002:8000`, монтирует `./archive` (архив конвертированных
файлов) и `./certs:/certs:ro` (сертификат). Поменять порт/IP — там же.

## Установка сертификата на устройства

Самоподписанный сертификат нужно одобрить один раз на каждом устройстве, иначе будет
предупреждение о недоверенном соединении (а на десктоп-приложениях — тихо неработающий
режим подключения к серверу без единой ошибки в UI):

- **iOS**: `https://ТВОЙ_IP:8002/cert` в Safari → скачать профиль → Настройки → установить →
  Настройки → Основные → Об этом устройстве → Настройки доверия сертификатов → включить
  полное доверие. После этого `https://ТВОЙ_IP:8002` → «Поделиться» → «На экран Домой».
- **Windows**: скачать `/cert`, `certutil -user -addstore "Root" cert.pem`.
- **macOS**: скачать `/cert`, открыть в Keychain Access → «Всегда доверять».

## PWA на iPhone/iPad

Подробное описание PWA-приложения для iOS (что это даёт, как выглядит, чем полезно) — в
[корневом README](../README.ru.md#как-получить). Здесь только техническая
сторона: сертификат (выше) и API (ниже).

## API

| Метод | Путь | Что делает |
|---|---|---|
| `POST` | `/api/convert` | Конвертирует загруженный файл, сохраняет в архив, возвращает markdown |
| `GET` | `/api/archive?q=` | Список архива (опциональный поиск по имени) |
| `GET` | `/api/archive/{filename}` | Скачать файл. `?raw=1` — `application/octet-stream` для десктоп-приложений (см. корневой README, раздел «Известные грабли»); без параметра — `text/markdown` для браузеров |
| `DELETE` | `/api/archive/{filename}` | Удалить файл из архива |
| `GET` | `/cert` | Отдаёт публичный сертификат для установки на устройство |

`markitdown[all]` тянет тяжёлые ML-зависимости (аудио-транскрипция и т.д.) — это осознанный
выбор для серверной версии, где место и время сборки не критичны. У десктоп-версии (см. корень
репозитория) набор extras урезан для portable-сборки.
