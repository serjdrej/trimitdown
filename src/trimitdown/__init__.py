"""TrimItDown: конвертация документов в markdown, пригодный для вставки в LLM.

ЕДИНСТВЕННЫЙ источник версии для всего проекта: пакета на PyPI, десктопного
приложения, ответа /api/mode и того, что видно в UI. Раньше версия приложения
жила отдельно в core/version.py -- два числа неизбежно разъезжаются, и разъезд
замечает пользователь, а не мы.

Обязан остаться строковым литералом: setuptools читает его статически через AST,
не импортируя модуль, -- иначе в build-окружение потянется markitdown.
"""

__version__ = "0.1.0"
